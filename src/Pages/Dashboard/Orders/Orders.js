import React from 'react';
import useTitle from '../../../Hooks/useTitle';

const Orders = () => {
    useTitle('Orders')
    return (
        <div>
            
        </div>
    );
};

export default Orders;